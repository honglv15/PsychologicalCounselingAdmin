<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.dhl.mapper.ConsultInfoMapper">

    <insert id="AddConsultInfo">
        insert into consult_info (admin_id, title_id, consult_school, consult_background, consult_intro, consult_fee)
        values (#{admId}, #{selectedVal}, #{school}, #{pbg}, #{brief}, #{cost})
    </insert>
    <insert id="AddConsultFiledInfo">
        insert into consult_field_info (consult_id, field_id) value ( #{admId},#{filedId})
    </insert>
</mapper>